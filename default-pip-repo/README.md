# Demo of configuring default Python package repositories using Terraform

For Serverless workloads, administrators are now able to [configure location of Python repositories](https://learn.microsoft.com/en-us/azure/databricks/compute/serverless/dependencies#configure-default-python-package-repositories) that will be used to find Python packages.  This is done by creating a secret scope with the predefined name and adding secrets with predefined names into it.

This Terraform module shows how to do it.  You need to configure `terraform.tfvars` with the following variables:

- `pip_index_url` - (required, string) the URL of PIP index that will be used by default.
- `pip_extra_index_urls` - (optional, string) the comma-separated list of URLs for additional of PIP indexes.
- `pip_index_url` - (optional, string) the SSL certificate of the PIP server to avoid certificate validation errors if server uses a certificate generated by custom certificate authority.

And then [configure authentication](https://registry.terraform.io/providers/databricks/databricks/latest/docs#authentication) to point to your workspace, and follow standard `terraform init/plan/apply` sequence.

<!-- BEGIN_TF_DOCS -->
## Requirements

No requirements.

## Providers

| Name | Version |
|------|---------|
| <a name="provider_databricks"></a> [databricks](#provider\_databricks) | 1.63.0 |

## Modules

No modules.

## Resources

| Name | Type |
|------|------|
| [databricks_secret.pip_extra_index_url](https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/secret) | resource |
| [databricks_secret.pip_index_url](https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/secret) | resource |
| [databricks_secret.pip_ssl_cert](https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/secret) | resource |
| [databricks_secret_acl.admins_acl](https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/secret_acl) | resource |
| [databricks_secret_acl.users_acl](https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/secret_acl) | resource |
| [databricks_secret_scope.default_pip_config](https://registry.terraform.io/providers/databricks/databricks/latest/docs/resources/secret_scope) | resource |

## Inputs

| Name | Description | Type | Default | Required |
|------|-------------|------|---------|:--------:|
| <a name="input_pip_index_url"></a> [pip\_index\_url](#input\_pip\_index\_url) | The PIP URL that will be used by default | `string` | n/a | yes |
| <a name="input_pip_extra_index_urls"></a> [pip\_extra\_index\_urls](#input\_pip\_extra\_index\_urls) | Optional comma-separated list of additional PIP URLs that will be used for package lookup | `string` | `""` | no |
| <a name="input_pip_ssl_cert"></a> [pip\_ssl\_cert](#input\_pip\_ssl\_cert) | Optional SSL certificate of the PIP server | `string` | `""` | no |

## Outputs

No outputs.
<!-- END_TF_DOCS -->